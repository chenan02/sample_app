To reset your password click the link below:

<%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

This link will self-destruct in 120 minutes.

If you did not request a password to be reset, this never happened.